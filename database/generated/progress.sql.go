// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: progress.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const listStudentProgressInputs = `-- name: ListStudentProgressInputs :many

WITH latest_required_ratings AS (
  SELECT DISTINCT ON (sa.class_id, sa.student_id, ai.competency_criterion_id)
    sa.class_id,
    sa.student_id,
    ai.competency_criterion_id,
    ai.rating
  FROM student_assessments sa
  JOIN assessment_items ai ON ai.assessment_id = sa.id
  JOIN competency_criteria cc
    ON cc.id = ai.competency_criterion_id
    AND cc.is_required
  WHERE sa.status IN ('submitted', 'locked')
  ORDER BY
    sa.class_id,
    sa.student_id,
    ai.competency_criterion_id,
    sa.assessment_no DESC,
    sa.id DESC
)
SELECT
  ce.class_id, c.class_code, c.name AS class_name, c.status AS class_status,
  ce.status AS enrollment_status,
  c.course_id, co.code AS course_code, co.name AS course_name,
  co.total_sessions, co.minimum_attendance_pct,
  (
    SELECT COUNT(*)::int
    FROM class_sessions cs
    WHERE cs.class_id = c.id
      AND cs.status IN ('completed', 'locked')
  ) AS completed_sessions,
  (
    SELECT COUNT(*)::int
    FROM attendance_records ar
    JOIN class_sessions attendance_session ON attendance_session.id = ar.class_session_id
    WHERE ar.class_id = c.id
      AND ar.student_id = ce.student_id
      AND attendance_session.status <> 'cancelled'
  ) AS attendance_records,
  (
    SELECT COUNT(*)::int
    FROM attendance_records ar
    JOIN class_sessions attendance_session ON attendance_session.id = ar.class_session_id
    WHERE ar.class_id = c.id
      AND ar.student_id = ce.student_id
      AND ar.status IN ('present', 'late')
      AND attendance_session.status <> 'cancelled'
  ) AS attended_sessions,
  (
    SELECT COUNT(*)::int
    FROM attendance_records ar
    JOIN class_sessions attendance_session ON attendance_session.id = ar.class_session_id
    WHERE ar.class_id = c.id
      AND ar.student_id = ce.student_id
      AND ar.status = 'excused'
      AND attendance_session.status <> 'cancelled'
  ) AS excused_sessions,
  (
    SELECT COUNT(*)::int
    FROM competency_criteria cc
    WHERE cc.course_id = c.course_id
      AND cc.is_required
  ) AS required_competencies,
  (
    SELECT COUNT(*)::int
    FROM latest_required_ratings lrr
    WHERE lrr.class_id = c.id
      AND lrr.student_id = ce.student_id
      AND lrr.rating IN ('competent', 'good', 'excellent')
  ) AS competencies_met,
  (
    SELECT COUNT(*)::int
    FROM class_sessions cs
    WHERE cs.class_id = c.id
      AND cs.session_type = 'assessment'
      AND cs.status <> 'cancelled'
  ) AS required_assessments,
  (
    SELECT COUNT(DISTINCT sa.session_id)::int
    FROM student_assessments sa
    JOIN class_sessions cs ON cs.id = sa.session_id
    WHERE sa.class_id = c.id
      AND sa.student_id = ce.student_id
      AND sa.status IN ('submitted', 'locked')
      AND cs.session_type = 'assessment'
      AND cs.status <> 'cancelled'
  ) AS completed_assessments
FROM class_enrollments ce
JOIN student_profiles sp ON sp.id = ce.student_id
JOIN classes c ON c.id = ce.class_id
JOIN courses co ON co.id = c.course_id
WHERE sp.user_id = $1
  AND (
    $2::uuid IS NULL
    OR ce.class_id = $2::uuid
  )
ORDER BY c.start_date DESC, c.id
`

type ListStudentProgressInputsParams struct {
	UserID  pgtype.UUID `json:"user_id"`
	ClassID pgtype.UUID `json:"class_id"`
}

type ListStudentProgressInputsRow struct {
	ClassID              pgtype.UUID      `json:"class_id"`
	ClassCode            string           `json:"class_code"`
	ClassName            string           `json:"class_name"`
	ClassStatus          ClassStatus      `json:"class_status"`
	EnrollmentStatus     EnrollmentStatus `json:"enrollment_status"`
	CourseID             pgtype.UUID      `json:"course_id"`
	CourseCode           string           `json:"course_code"`
	CourseName           string           `json:"course_name"`
	TotalSessions        int32            `json:"total_sessions"`
	MinimumAttendancePct pgtype.Numeric   `json:"minimum_attendance_pct"`
	CompletedSessions    int32            `json:"completed_sessions"`
	AttendanceRecords    int32            `json:"attendance_records"`
	AttendedSessions     int32            `json:"attended_sessions"`
	ExcusedSessions      int32            `json:"excused_sessions"`
	RequiredCompetencies int32            `json:"required_competencies"`
	CompetenciesMet      int32            `json:"competencies_met"`
	RequiredAssessments  int32            `json:"required_assessments"`
	CompletedAssessments int32            `json:"completed_assessments"`
}

// Deterministic Student progress inputs. Business percentages and eligibility
// are calculated in Go from these integer/decimal components.
func (q *Queries) ListStudentProgressInputs(ctx context.Context, arg ListStudentProgressInputsParams) ([]ListStudentProgressInputsRow, error) {
	rows, err := q.db.Query(ctx, listStudentProgressInputs, arg.UserID, arg.ClassID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListStudentProgressInputsRow{}
	for rows.Next() {
		var i ListStudentProgressInputsRow
		if err := rows.Scan(
			&i.ClassID,
			&i.ClassCode,
			&i.ClassName,
			&i.ClassStatus,
			&i.EnrollmentStatus,
			&i.CourseID,
			&i.CourseCode,
			&i.CourseName,
			&i.TotalSessions,
			&i.MinimumAttendancePct,
			&i.CompletedSessions,
			&i.AttendanceRecords,
			&i.AttendedSessions,
			&i.ExcusedSessions,
			&i.RequiredCompetencies,
			&i.CompetenciesMet,
			&i.RequiredAssessments,
			&i.CompletedAssessments,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
