# ==========================================================================
# NSA Training Platform API — OpenAPI contract (SOURCE OF TRUTH)
# Update this file in the SAME phase as the API change it describes.
# View locally: make swagger  →  http://localhost:8081
# ==========================================================================
openapi: 3.1.0

info:
  title: NSA Training Platform API
  version: "1.1.0"
  description: |
    REST/JSON API for the NSA Training Platform (modular monolith).
    Base path: `/api/v1`. All timestamps are UTC (RFC 3339).

    **Roles:** ADMIN, TEACHER, STUDENT (RBAC + ownership checks).

    **Response envelopes**
    - Success: `{ "data": ..., "message": "Success" }`
    - Error: `{ "error": { "code": "SOME_CODE", "message": "...", "details": [] } }`

    **Implementation status:** Phase 16 — system probes, authentication/RBAC,
    academic-core administration, training locations, class-session scheduling,
    role-scoped schedules, batch attendance, corrections, locking, and Student
    temporally correct attendance history/summary with absence-risk warnings
    are live. Practical assessments with evidence, deterministic Student progress,
    ADMIN completion decisions, verifiable PDF certificates, in-app notifications,
    operational CSV reports, and assignment-scoped Teacher workspaces are live.

    **Authentication:** `POST /api/v1/auth/login` returns a short-lived JWT
    access token (use as `Authorization: Bearer <token>`) and sets an
    HttpOnly `nsa_refresh` cookie (path `/api/v1/auth`) used by
    `/auth/refresh` and `/auth/logout`. Refresh tokens rotate on every use;
    reuse of a revoked token revokes the entire session family.

servers:
  - url: http://localhost:8080
    description: Local development (business endpoints are prefixed /api/v1)

tags:
  - name: system
    description: Operational endpoints (no authentication)
  - name: auth
    description: Authentication and session management (Phase 3)
  - name: admin
    description: Administrator management endpoints (Phase 4+)
  - name: teacher
    description: Teacher schedule and assigned-class endpoints (Phase 5+)
  - name: student
    description: Student schedule and self-service endpoints (Phase 5+)

paths:
  /health:
    get:
      tags: [system]
      summary: Liveness probe
      description: Returns 200 when the API process is up. Does not check dependencies.
      operationId: getHealth
      security: []
      responses:
        "200":
          description: API is alive
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
              example:
                data: { status: "ok" }
                message: Success

  /ready:
    get:
      tags: [system]
      summary: Readiness probe
      description: Returns 200 only when the API can serve traffic (database reachable); otherwise 503.
      operationId: getReady
      security: []
      responses:
        "200":
          description: API is ready (database reachable)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
              example:
                data: { status: "ready", database: "up" }
                message: Success
        "503":
          description: API is not ready (database unavailable)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: SERVICE_UNAVAILABLE
                  message: Database is not reachable
                  details: []

  /api/v1/auth/login:
    post:
      tags: [auth]
      summary: Log in with email and password
      description: Rate-limited (10 req/min per IP). Invalid credentials always return the same generic 401 (no user enumeration).
      operationId: login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login successful. Refresh token is set as an HttpOnly cookie.
          headers:
            Set-Cookie:
              description: "nsa_refresh=<opaque>; Path=/api/v1/auth; HttpOnly; SameSite=Lax (Secure outside development)"
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenBundleResponse"
        "400":
          description: Validation error
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "401":
          description: Invalid credentials (generic)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
              example:
                error: { code: INVALID_CREDENTIALS, message: "Invalid email or password" }
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/auth/refresh:
    post:
      tags: [auth]
      summary: Rotate the refresh token and get a new access token
      description: Reads the `nsa_refresh` HttpOnly cookie. Rotates the token on every call; presenting a revoked token revokes the whole session family.
      operationId: refresh
      security: []
      responses:
        "200":
          description: New token bundle issued
          headers:
            Set-Cookie:
              description: Rotated nsa_refresh cookie
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TokenBundleResponse"
        "401":
          description: Refresh token missing/invalid/expired/revoked
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
              example:
                error: { code: INVALID_REFRESH_TOKEN, message: "Refresh token is invalid or expired" }

  /api/v1/auth/logout:
    post:
      tags: [auth]
      summary: Log out (revoke the refresh token)
      operationId: logout
      security: []
      responses:
        "200":
          description: Logged out (idempotent); refresh cookie cleared
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /api/v1/auth/change-password:
    post:
      tags: [auth]
      summary: Change the current user's password
      description: Requires a valid access token. On success all refresh tokens are revoked (re-login required). New password must be at least 8 characters.
      operationId: changePassword
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangePasswordRequest"
      responses:
        "200":
          description: Password changed; all sessions revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          description: WRONG_CURRENT_PASSWORD or WEAK_PASSWORD
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "401":
          description: Not authenticated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/auth/me:
    get:
      tags: [auth]
      summary: Get the authenticated user's profile
      operationId: getMe
      responses:
        "200":
          description: Current user info
          content:
            application/json:
              schema:
                type: object
                required: [data, message]
                properties:
                  data: { $ref: "#/components/schemas/UserInfo" }
                  message: { type: string, example: Success }
        "401":
          description: Not authenticated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/admin/students:
    get:
      tags: [admin]
      summary: List students
      operationId: listAdminStudents
      parameters:
        - { $ref: "#/components/parameters/Search" }
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/StudentStatus" }
      responses:
        "200":
          description: Paginated students
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentListResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      summary: Create a student account and profile
      operationId: createAdminStudent
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateStudentRequest" }
      responses:
        "201":
          description: Student created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/students/{studentID}:
    parameters:
      - { $ref: "#/components/parameters/StudentID" }
    get:
      tags: [admin]
      summary: Get a student
      operationId: getAdminStudent
      responses:
        "200":
          description: Student
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [admin]
      summary: Replace student account and profile fields
      operationId: updateAdminStudent
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateStudentRequest" }
      responses:
        "200":
          description: Student updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/students/{studentID}/status-history:
    parameters:
      - { $ref: "#/components/parameters/StudentID" }
    get:
      tags: [admin]
      summary: List immutable student lifecycle transitions
      operationId: listAdminStudentStatusHistory
      responses:
        "200":
          description: Newest-first status history
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentStatusHistoryResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/students/export:
    get:
      tags: [admin]
      summary: Export the filtered student directory as UTF-8 CSV
      operationId: exportAdminStudents
      parameters:
        - { $ref: "#/components/parameters/Search" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/StudentStatus" }
      responses:
        "200":
          description: Student CSV with a blank temporary_password template column
          content:
            text/csv:
              schema: { type: string }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/students/import:
    post:
      tags: [admin]
      summary: Import student profiles from UTF-8 CSV with row-level results
      operationId: importAdminStudents
      requestBody:
        required: true
        content:
          text/csv:
            schema: { type: string, maxLength: 2097152 }
      responses:
        "200":
          description: Import result; valid rows may succeed when other rows fail validation
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentImportResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/teachers:
    get:
      tags: [admin]
      summary: List teachers
      operationId: listAdminTeachers
      parameters:
        - { $ref: "#/components/parameters/Search" }
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/TeacherStatus" }
      responses:
        "200":
          description: Paginated teachers
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeacherListResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      summary: Create a teacher account and profile
      operationId: createAdminTeacher
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTeacherRequest" }
      responses:
        "201":
          description: Teacher created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeacherResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/teachers/{teacherID}:
    parameters:
      - { $ref: "#/components/parameters/TeacherID" }
    get:
      tags: [admin]
      summary: Get a teacher
      operationId: getAdminTeacher
      responses:
        "200":
          description: Teacher
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeacherResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [admin]
      summary: Replace teacher account and profile fields
      operationId: updateAdminTeacher
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateTeacherRequest" }
      responses:
        "200":
          description: Teacher updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeacherResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/courses:
    get:
      tags: [admin]
      summary: List courses
      operationId: listAdminCourses
      parameters:
        - { $ref: "#/components/parameters/Search" }
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/CourseStatus" }
      responses:
        "200":
          description: Paginated courses
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseListResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      summary: Create a course
      operationId: createAdminCourse
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CourseWriteRequest" }
      responses:
        "201":
          description: Course created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/courses/{courseID}:
    parameters:
      - { $ref: "#/components/parameters/CourseID" }
    get:
      tags: [admin]
      summary: Get a course
      operationId: getAdminCourse
      responses:
        "200":
          description: Course
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [admin]
      summary: Replace course fields
      operationId: updateAdminCourse
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CourseWriteRequest" }
      responses:
        "200":
          description: Course updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/courses/{courseID}/modules:
    parameters:
      - { $ref: "#/components/parameters/CourseID" }
    get:
      tags: [admin]
      summary: List course modules
      operationId: listAdminCourseModules
      responses:
        "200":
          description: Ordered modules
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModuleListResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [admin]
      summary: Create a course module
      operationId: createAdminCourseModule
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ModuleWriteRequest" }
      responses:
        "201":
          description: Module created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModuleResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/courses/{courseID}/modules/{moduleID}:
    parameters:
      - { $ref: "#/components/parameters/CourseID" }
      - { $ref: "#/components/parameters/ModuleID" }
    put:
      tags: [admin]
      summary: Replace course module fields
      operationId: updateAdminCourseModule
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ModuleWriteRequest" }
      responses:
        "200":
          description: Module updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModuleResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/courses/{courseID}/competencies:
    parameters:
      - { $ref: "#/components/parameters/CourseID" }
    get:
      tags: [admin]
      summary: List competency criteria
      operationId: listAdminCompetencies
      responses:
        "200":
          description: Ordered competency criteria
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CompetencyListResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [admin]
      summary: Create a competency criterion
      operationId: createAdminCompetency
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompetencyWriteRequest" }
      responses:
        "201":
          description: Competency created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CompetencyResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/courses/{courseID}/competencies/{criterionID}:
    parameters:
      - { $ref: "#/components/parameters/CourseID" }
      - { $ref: "#/components/parameters/CriterionID" }
    put:
      tags: [admin]
      summary: Replace competency criterion fields
      operationId: updateAdminCompetency
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompetencyWriteRequest" }
      responses:
        "200":
          description: Competency updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CompetencyResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/courses/{courseID}/tests:
    parameters:
      - { name: courseID, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [admin]
      summary: List configured in-class tests and the final exam
      operationId: listAdminCourseTests
      responses:
        "200": { description: Course tests, content: { application/json: { schema: { $ref: "#/components/schemas/CourseTestListResponse" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [admin]
      summary: Configure a course test
      operationId: createAdminCourseTest
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CourseTestRequest" } } }
      responses:
        "201": { description: Test created, content: { application/json: { schema: { $ref: "#/components/schemas/CourseTestResponse" } } } }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/courses/{courseID}/tests/{testID}:
    put:
      tags: [admin]
      summary: Replace a configured course test
      operationId: updateAdminCourseTest
      parameters:
        - { name: courseID, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: testID, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CourseTestRequest" } } }
      responses:
        "200": { description: Test updated, content: { application/json: { schema: { $ref: "#/components/schemas/CourseTestResponse" } } } }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/classes:
    get:
      tags: [admin]
      summary: List classes
      operationId: listAdminClasses
      parameters:
        - { $ref: "#/components/parameters/Search" }
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/ClassStatus" }
        - name: course_id
          in: query
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Paginated classes
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ClassListResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      summary: Create a class
      operationId: createAdminClass
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ClassWriteRequest" }
      responses:
        "201":
          description: Class created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ClassResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/classes/{classID}:
    parameters:
      - { $ref: "#/components/parameters/ClassID" }
    get:
      tags: [admin]
      summary: Get a class
      operationId: getAdminClass
      responses:
        "200":
          description: Class
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ClassResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [admin]
      summary: Replace class fields
      operationId: updateAdminClass
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ClassWriteRequest" }
      responses:
        "200":
          description: Class updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ClassResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/classes/{classID}/enrollments:
    parameters:
      - { $ref: "#/components/parameters/ClassID" }
    get:
      tags: [admin]
      summary: List class enrollments
      operationId: listAdminClassEnrollments
      responses:
        "200":
          description: Class enrollments
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrollmentListResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [admin]
      summary: Enroll a student
      operationId: createAdminClassEnrollment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnrollmentCreateRequest" }
      responses:
        "201":
          description: Student enrolled
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrollmentResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/classes/{classID}/operation-history:
    parameters:
      - { $ref: "#/components/parameters/ClassID" }
    get:
      tags: [admin]
      summary: List immutable class operation history
      operationId: listAdminClassOperationHistory
      responses:
        "200":
          description: Class operation history ordered from newest to oldest
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ClassOperationHistoryListResponse" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/classes/{classID}/enrollments/{enrollmentID}:
    parameters:
      - { $ref: "#/components/parameters/ClassID" }
      - { $ref: "#/components/parameters/EnrollmentID" }
    put:
      tags: [admin]
      summary: Change enrollment status
      operationId: updateAdminClassEnrollment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnrollmentUpdateRequest" }
      responses:
        "200":
          description: Enrollment updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrollmentResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/classes/{classID}/enrollments/{enrollmentID}/transfer:
    parameters:
      - { $ref: "#/components/parameters/ClassID" }
      - { $ref: "#/components/parameters/EnrollmentID" }
    post:
      tags: [admin]
      summary: Atomically transfer an active enrollment to another class of the same course
      operationId: transferAdminClassEnrollment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnrollmentTransferRequest" }
      responses:
        "200":
          description: Enrollment transferred
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrollmentTransferResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/classes/{classID}/teacher-assignments:
    parameters:
      - { $ref: "#/components/parameters/ClassID" }
    get:
      tags: [admin]
      summary: List teacher assignments
      operationId: listAdminTeacherAssignments
      responses:
        "200":
          description: Teacher assignments
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssignmentListResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [admin]
      summary: Assign a teacher to a class
      operationId: createAdminTeacherAssignment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignmentCreateRequest" }
      responses:
        "201":
          description: Teacher assigned
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssignmentResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/classes/{classID}/teacher-assignments/{assignmentID}:
    parameters:
      - { $ref: "#/components/parameters/ClassID" }
      - { $ref: "#/components/parameters/AssignmentID" }
    put:
      tags: [admin]
      summary: Change a teacher assignment role
      operationId: updateAdminTeacherAssignment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssignmentUpdateRequest" }
      responses:
        "200":
          description: Assignment updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssignmentResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    delete:
      tags: [admin]
      summary: Remove a teacher assignment
      operationId: deleteAdminTeacherAssignment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ReasonRequest" }
      responses:
        "200":
          description: Assignment removed
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessageResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/locations:
    get:
      tags: [admin]
      summary: List training locations
      operationId: listAdminTrainingLocations
      parameters:
        - { $ref: "#/components/parameters/Search" }
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - name: is_active
          in: query
          schema: { type: boolean }
      responses:
        "200":
          description: Paginated training locations
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TrainingLocationListResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      summary: Create a training location
      operationId: createAdminTrainingLocation
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTrainingLocationRequest" }
      responses:
        "201":
          description: Training location created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TrainingLocationResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/locations/{locationID}:
    parameters:
      - { $ref: "#/components/parameters/LocationID" }
    get:
      tags: [admin]
      summary: Get a training location
      operationId: getAdminTrainingLocation
      responses:
        "200":
          description: Training location
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TrainingLocationResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [admin]
      summary: Replace training location fields
      operationId: updateAdminTrainingLocation
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateTrainingLocationRequest" }
      responses:
        "200":
          description: Training location updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TrainingLocationResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/sessions:
    get:
      tags: [admin]
      summary: List class sessions
      description: Time filters select sessions overlapping the half-open window `[from,to)`.
      operationId: listAdminClassSessions
      parameters:
        - { $ref: "#/components/parameters/Search" }
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - { $ref: "#/components/parameters/FromTime" }
        - { $ref: "#/components/parameters/ToTime" }
        - name: status
          in: query
          schema: { $ref: "#/components/schemas/SessionStatus" }
        - name: class_id
          in: query
          schema: { type: string, format: uuid }
        - name: teacher_id
          in: query
          schema: { type: string, format: uuid }
        - name: location_id
          in: query
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Paginated class sessions
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionListResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      summary: Create a class session
      description: |
        `starts_at` and `ends_at` accept RFC3339 offsets and are stored/returned
        in UTC. The class date range and fixed training slot are evaluated in
        Asia/Ho_Chi_Minh. The accepted local slots are 08:00–12:00,
        13:30–17:30, and 18:30–21:30.
      operationId: createAdminClassSession
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SessionWriteRequest" }
      responses:
        "201":
          description: Class session created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/sessions/{sessionID}:
    parameters:
      - { $ref: "#/components/parameters/SessionID" }
    get:
      tags: [admin]
      summary: Get a class session
      operationId: getAdminClassSession
      responses:
        "200":
          description: Class session
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionResponse" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [admin]
      summary: Replace class session fields
      description: Locked sessions cannot be changed.
      operationId: updateAdminClassSession
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SessionWriteRequest" }
      responses:
        "200":
          description: Class session updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/sessions/{sessionID}/attendance:
    parameters:
      - { $ref: "#/components/parameters/SessionID" }
    get:
      tags: [admin]
      summary: View a session attendance roster
      description: Includes every actively enrolled student and any recorded status.
      operationId: getAdminSessionAttendance
      responses:
        "200":
          description: Session attendance roster and counts
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionAttendanceResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/attendance/{attendanceID}:
    parameters:
      - { $ref: "#/components/parameters/AttendanceID" }
    put:
      tags: [admin]
      summary: Correct an attendance record
      description: |
        Administrative correction is allowed after attendance is locked.
        A non-empty reason is required and is preserved with old/new values
        in the audit log.
      operationId: correctAdminAttendance
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AttendanceCorrectionRequest" }
      responses:
        "200":
          description: Corrected attendance record
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AttendanceRecordResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/teacher/classes:
    get:
      tags: [teacher]
      summary: List classes assigned to the authenticated teacher
      operationId: listTeacherClasses
      responses:
        "200":
          description: Assigned classes
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeacherClassListResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/teacher/classes/{classID}:
    parameters:
      - { $ref: "#/components/parameters/ClassID" }
    get:
      tags: [teacher]
      summary: Get an assigned class with its students and competencies
      operationId: getTeacherClass
      responses:
        "200":
          description: Assigned class workspace data
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TeacherClassDetailResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/teacher/schedule:
    get:
      tags: [teacher]
      summary: Get the authenticated teacher's assigned schedule
      operationId: getTeacherSchedule
      parameters:
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - { $ref: "#/components/parameters/FromTime" }
        - { $ref: "#/components/parameters/ToTime" }
      responses:
        "200":
          description: Sessions assigned to the authenticated teacher
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionListResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/teacher/sessions/{sessionID}/attendance:
    parameters:
      - { $ref: "#/components/parameters/SessionID" }
    get:
      tags: [teacher]
      summary: View attendance for an assigned class session
      operationId: getTeacherSessionAttendance
      responses:
        "200":
          description: Session attendance roster and counts
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionAttendanceResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [teacher]
      summary: Save or update a batch of attendance during the session day
      description: |
        The authenticated teacher must be assigned to the session's class.
        The entire batch is transactional. Every student must have an active
        enrollment and student IDs must be unique inside the request. Existing
        records are updated. At 00:00 Asia/Ho_Chi_Minh on the next day, missing
        records are saved as Absent and the session is automatically locked.
      operationId: recordTeacherSessionAttendance
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AttendanceBatchRequest" }
      responses:
        "200":
          description: Attendance batch saved
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AttendanceBatchResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/UnprocessableEntity" }

  /api/v1/teacher/classes/{classID}/students/{studentID}/assessments:
    parameters:
      - { $ref: "#/components/parameters/ClassID" }
      - { $ref: "#/components/parameters/StudentID" }
    get:
      tags: [teacher]
      summary: List a student's assessment history in an assigned class
      description: Drafts are visible to assigned teachers; results are ordered by assessment number descending.
      operationId: listTeacherStudentAssessments
      parameters:
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
      responses:
        "200":
          description: Paginated assessment history
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentListResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/UnprocessableEntity" }
    post:
      tags: [teacher]
      summary: Create a draft practical skill assessment
      description: |
        The authenticated teacher must be assigned to the class and the
        student must be actively enrolled. Assessment numbers are allocated
        transactionally per class/student. Every criterion must belong to the
        class course.
      operationId: createTeacherStudentAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssessmentWriteRequest" }
      responses:
        "201":
          description: Draft assessment created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/UnprocessableEntity" }

  /api/v1/teacher/assessments/{assessmentID}:
    parameters:
      - { $ref: "#/components/parameters/AssessmentID" }
    get:
      tags: [teacher]
      summary: Get an assessment in an assigned class
      operationId: getTeacherAssessment
      responses:
        "200":
          description: Assessment with competency items
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [teacher]
      summary: Replace a draft assessment
      description: Only the teacher who created the assessment may replace it, and only while it is draft.
      operationId: updateTeacherAssessment
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AssessmentWriteRequest" }
      responses:
        "200":
          description: Draft assessment updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/teacher/assessments/{assessmentID}/submit:
    parameters:
      - { $ref: "#/components/parameters/AssessmentID" }
    post:
      tags: [teacher]
      summary: Submit a draft assessment
      description: Every required course competency must have a rating other than Not Assessed.
      operationId: submitTeacherAssessment
      responses:
        "200":
          description: Assessment submitted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/teacher/assessments/{assessmentID}/lock:
    parameters:
      - { $ref: "#/components/parameters/AssessmentID" }
    post:
      tags: [teacher]
      summary: Lock a submitted assessment
      description: Locked assessments are immutable and retained as history.
      operationId: lockTeacherAssessment
      responses:
        "200":
          description: Assessment locked
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/teacher/classes/{classID}/students/{studentID}/test-results:
    get:
      tags: [teacher]
      summary: List course tests and all score attempts for an assigned student
      operationId: listTeacherStudentTestResults
      parameters:
        - { name: classID, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: studentID, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Student test results, content: { application/json: { schema: { $ref: "#/components/schemas/CourseTestResultsResponse" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/teacher/classes/{classID}/students/{studentID}/tests/{testID}/attempts:
    post:
      tags: [teacher]
      summary: Record a new score attempt for an actively enrolled student
      operationId: createTeacherStudentTestAttempt
      parameters:
        - { name: classID, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: studentID, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: testID, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/TestAttemptCreateRequest" } } }
      responses:
        "201": { description: Score recorded, content: { application/json: { schema: { $ref: "#/components/schemas/TestAttemptResponse" } } } }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/teacher/test-attempts/{attemptID}:
    put:
      tags: [teacher]
      summary: Correct a score with an immutable reason trail
      operationId: correctTeacherTestAttempt
      parameters:
        - { name: attemptID, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/TestAttemptCorrectionRequest" } } }
      responses:
        "200": { description: Score corrected, content: { application/json: { schema: { $ref: "#/components/schemas/TestAttemptResponse" } } } }
        "400": { $ref: "#/components/responses/ValidationError" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/teacher/test-attempts/{attemptID}/history:
    get:
      tags: [teacher]
      summary: List score correction history
      operationId: listTeacherTestAttemptHistory
      parameters:
        - { name: attemptID, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Immutable score history, content: { application/json: { schema: { $ref: "#/components/schemas/TestAttemptHistoryListResponse" } } } }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/student/schedule:
    get:
      tags: [student]
      summary: Get the authenticated student's enrolled-class schedule
      operationId: getStudentSchedule
      parameters:
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - { $ref: "#/components/parameters/FromTime" }
        - { $ref: "#/components/parameters/ToTime" }
      responses:
        "200":
          description: Sessions for the authenticated student's active enrollment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionListResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/student/sessions/{sessionID}/attendance:
    parameters:
      - { $ref: "#/components/parameters/SessionID" }
    get:
      tags: [student]
      summary: View attendance statuses for classmates in an enrolled class
      description: |
        Returns student names and attendance statuses for the session roster.
        Internal notes, recorder identities, and attendance record IDs are hidden.
      operationId: getStudentSessionAttendance
      responses:
        "200":
          description: Class attendance roster visible to an enrolled student
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SessionAttendanceResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/student/attendance:
    get:
      tags: [student]
      summary: Get the authenticated student's attendance history
      operationId: getStudentAttendanceHistory
      parameters:
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - name: class_id
          in: query
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Paginated attendance history for the authenticated student only
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentAttendanceHistoryResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/student/attendance/summary:
    get:
      tags: [student]
      summary: Get the authenticated student's per-class attendance summary
      description: |
        `attendance_pct` counts Present and Late as attended and excludes
        Excused records from the denominator. This is informational; Phase 7
        defines completion calculations.
      operationId: getStudentAttendanceSummary
      parameters:
        - name: class_id
          in: query
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Per-class attendance summaries for the authenticated student only
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentAttendanceSummaryResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/student/assessments:
    get:
      tags: [student]
      summary: Get the authenticated student's submitted assessment history
      operationId: listStudentAssessments
      parameters:
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - name: class_id
          in: query
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Paginated submitted/locked assessments for the authenticated student only
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentListResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/student/assessments/{assessmentID}:
    parameters:
      - { $ref: "#/components/parameters/AssessmentID" }
    get:
      tags: [student]
      summary: Get one of the authenticated student's assessments
      operationId: getStudentAssessment
      responses:
        "200":
          description: Submitted or locked assessment owned by the authenticated student
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AssessmentResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/student/progress:
    get:
      tags: [student]
      summary: Get the authenticated student's learning progress dashboard
      description: |
        Completion eligibility is calculated per student/course and survives
        same-course class transfers. It requires attendance of at least 80%,
        every active mandatory in-class test passed, and one active final exam
        with a best score strictly greater than 5. Session, competency, and
        practical-assessment metrics remain visible as learning-progress context.
      operationId: getStudentProgress
      parameters:
        - name: class_id
          in: query
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Progress dashboard for the authenticated student only
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProgressDashboardResponse" }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/student/test-results:
    get:
      tags: [student]
      summary: List the authenticated student's scores grouped by course
      operationId: listStudentTestResults
      responses:
        "200":
          description: Own test results, including attempts retained across same-course transfers
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StudentCourseTestResultsResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/completions:
    get:
      tags: [admin]
      summary: List course-completion candidates and calculated eligibility
      operationId: listCompletionCandidates
      parameters:
        - { $ref: "#/components/parameters/Page" }
        - { $ref: "#/components/parameters/PerPage" }
        - { name: search, in: query, schema: { type: string } }
      responses:
        "200": { description: Completion candidates, content: { application/json: { schema: { $ref: "#/components/schemas/CompletionCandidateListResponse" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/test-attempts/{attemptID}:
    put:
      tags: [admin]
      summary: Administratively correct any score with a required reason
      operationId: correctAdminTestAttempt
      parameters:
        - { name: attemptID, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/TestAttemptCorrectionRequest" } } }
      responses:
        "200": { description: Score corrected, content: { application/json: { schema: { $ref: "#/components/schemas/TestAttemptResponse" } } } }
        "400": { $ref: "#/components/responses/ValidationError" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/test-attempts/{attemptID}/history:
    get:
      tags: [admin]
      summary: List immutable score correction history
      operationId: listAdminTestAttemptHistory
      parameters:
        - { name: attemptID, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Score correction history, content: { application/json: { schema: { $ref: "#/components/schemas/TestAttemptHistoryListResponse" } } } }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/completions/{classID}/{studentID}:
    put:
      tags: [admin]
      summary: Approve or reject course completion
      operationId: decideCourseCompletion
      parameters:
        - { name: classID, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: studentID, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompletionDecisionRequest" }
      responses:
        "200": { description: Saved decision and optional certificate, content: { application/json: { schema: { $ref: "#/components/schemas/CompletionDecisionResponse" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/reports/summary:
    get:
      tags: [admin]
      summary: Get operational dashboard metrics
      operationId: getOperationalReportSummary
      responses:
        "200": { description: Operational metrics, content: { application/json: { schema: { $ref: "#/components/schemas/ReportSummaryResponse" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/completions/{classID}/{studentID}/history:
    get:
      tags: [admin]
      summary: List immutable completion decision history
      operationId: listCompletionDecisionHistory
      parameters:
        - { name: classID, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: studentID, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Decision history }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/certificates/{certificateID}/pdf:
    get:
      tags: [admin]
      summary: Download a current certificate PDF
      operationId: downloadAdminCertificate
      parameters:
        - { name: certificateID, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Certificate PDF, content: { application/pdf: { schema: { type: string, format: binary } } } }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/certificates/{certificateID}/revoke:
    post:
      tags: [admin]
      summary: Revoke a current certificate
      operationId: revokeCertificate
      parameters:
        - { name: certificateID, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CertificateActionRequest" } } }
      responses:
        "200": { description: Revoked certificate, content: { application/json: { schema: { $ref: "#/components/schemas/CertificateResponse" } } } }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/certificates/{certificateID}/reissue:
    post:
      tags: [admin]
      summary: Revoke and replace a current certificate
      operationId: reissueCertificate
      parameters:
        - { name: certificateID, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/CertificateActionRequest" } } }
      responses:
        "200": { description: Replacement certificate, content: { application/json: { schema: { $ref: "#/components/schemas/CertificateResponse" } } } }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/reports/{reportKind}.csv:
    get:
      tags: [admin]
      summary: Export an operational report as UTF-8 CSV
      operationId: exportOperationalReport
      parameters:
        - { name: reportKind, in: path, required: true, schema: { type: string, enum: [attendance, competencies, classes, completions] } }
      responses:
        "200": { description: CSV file, content: { text/csv: { schema: { type: string } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/student/certificates:
    get:
      tags: [student]
      summary: List the authenticated student's certificates
      operationId: listStudentCertificates
      responses:
        "200": { description: Certificate history, content: { application/json: { schema: { $ref: "#/components/schemas/CertificateListResponse" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/student/certificates/{certificateID}/pdf:
    get:
      tags: [student]
      summary: Download an owned current certificate PDF
      operationId: downloadStudentCertificate
      parameters:
        - { name: certificateID, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Certificate PDF, content: { application/pdf: { schema: { type: string, format: binary } } } }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/certificates/{verificationCode}:
    get:
      tags: [system]
      security: []
      summary: Publicly verify a certificate, including revocation state
      operationId: verifyCertificate
      parameters:
        - { name: verificationCode, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Certificate verification record, content: { application/json: { schema: { $ref: "#/components/schemas/CertificateResponse" } } } }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/notifications:
    get:
      tags: [student, teacher, admin]
      summary: List authenticated user's in-app notifications
      operationId: listNotifications
      responses:
        "200": { description: Notifications and unread count, content: { application/json: { schema: { $ref: "#/components/schemas/NotificationListResponse" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/notifications/{notificationID}/read:
    put:
      tags: [student, teacher, admin]
      summary: Mark an owned notification read
      operationId: markNotificationRead
      parameters:
        - { name: notificationID, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Updated notification }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/notifications/{notificationID}:
    delete:
      tags: [student, teacher, admin]
      summary: Archive an owned notification
      operationId: archiveNotification
      parameters:
        - { name: notificationID, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Archived notification }
        "404": { $ref: "#/components/responses/NotFound" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Short-lived access token (Phase 3). Refresh token is an HttpOnly cookie.

  parameters:
    Search:
      name: search
      in: query
      schema: { type: string, maxLength: 200 }
    Page:
      name: page
      in: query
      schema: { type: integer, minimum: 1, default: 1 }
    PerPage:
      name: per_page
      in: query
      schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
    StudentID:
      name: studentID
      in: path
      required: true
      schema: { type: string, format: uuid }
    TeacherID:
      name: teacherID
      in: path
      required: true
      schema: { type: string, format: uuid }
    CourseID:
      name: courseID
      in: path
      required: true
      schema: { type: string, format: uuid }
    ModuleID:
      name: moduleID
      in: path
      required: true
      schema: { type: string, format: uuid }
    CriterionID:
      name: criterionID
      in: path
      required: true
      schema: { type: string, format: uuid }
    ClassID:
      name: classID
      in: path
      required: true
      schema: { type: string, format: uuid }
    EnrollmentID:
      name: enrollmentID
      in: path
      required: true
      schema: { type: string, format: uuid }
    AssignmentID:
      name: assignmentID
      in: path
      required: true
      schema: { type: string, format: uuid }
    LocationID:
      name: locationID
      in: path
      required: true
      schema: { type: string, format: uuid }
    SessionID:
      name: sessionID
      in: path
      required: true
      schema: { type: string, format: uuid }
    AttendanceID:
      name: attendanceID
      in: path
      required: true
      schema: { type: string, format: uuid }
    AssessmentID:
      name: assessmentID
      in: path
      required: true
      schema: { type: string, format: uuid }
    FromTime:
      name: from
      in: query
      description: Inclusive RFC3339 window start; omitted means unbounded.
      schema: { type: string, format: date-time }
    ToTime:
      name: to
      in: query
      description: Exclusive RFC3339 window end; omitted means unbounded.
      schema: { type: string, format: date-time }

  responses:
    ValidationError:
      description: Request validation failed
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorResponse" }
    Unauthorized:
      description: Missing or invalid access token
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorResponse" }
    Forbidden:
      description: Required role, assignment, or ownership check failed
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorResponse" }
    NotFound:
      description: Referenced resource not found
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorResponse" }
    Conflict:
      description: Unique, capacity, state, or relationship conflict
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorResponse" }
    UnprocessableEntity:
      description: Referenced student is not actively enrolled in the session class
      content:
        application/json:
          schema: { $ref: "#/components/schemas/ErrorResponse" }

  schemas:
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email, example: admin@nsa.local }
        password: { type: string, format: password, example: NsaDemo@123 }

    ChangePasswordRequest:
      type: object
      required: [current_password, new_password]
      properties:
        current_password: { type: string, format: password }
        new_password: { type: string, format: password, minLength: 8 }

    ProfileRef:
      type: object
      properties:
        id: { type: string, format: uuid }
        code: { type: string, example: STU-DEMO-001 }
        full_name: { type: string, example: Demo Student }

    UserInfo:
      type: object
      properties:
        id: { type: string, format: uuid }
        email: { type: string, example: admin@nsa.local }
        roles:
          type: array
          items: { type: string, enum: [ADMIN, TEACHER, STUDENT] }
          example: [ADMIN]
        must_change_password: { type: boolean, example: false }
        student_profile: { $ref: "#/components/schemas/ProfileRef" }
        teacher_profile: { $ref: "#/components/schemas/ProfileRef" }

    TokenBundleResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          properties:
            access_token: { type: string }
            token_type: { type: string, example: Bearer }
            access_expires_at: { type: string, format: date-time }
            must_change_password: { type: boolean }
            user: { $ref: "#/components/schemas/UserInfo" }
        message: { type: string, example: Success }

    MessageResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          properties:
            message: { type: string, example: "Logged out" }
        message: { type: string, example: Success }

    HealthResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          additionalProperties: true
        message:
          type: string
          example: Success

    UserStatus:
      type: string
      enum: [pending, active, suspended, inactive]
    StudentStatus:
      type: string
      enum: [pending, active, suspended, completed, withdrawn]
    TeacherStatus:
      type: string
      enum: [active, inactive]
    CourseStatus:
      type: string
      enum: [draft, active, inactive, archived]
    ClassStatus:
      type: string
      enum: [planning, open, in_progress, completed, cancelled, archived]
    EnrollmentStatus:
      type: string
      enum: [enrolled, transferred, completed, withdrawn]

    PaginationMeta:
      type: object
      required: [page, per_page, total, total_pages]
      properties:
        page: { type: integer, minimum: 1 }
        per_page: { type: integer, minimum: 1, maximum: 100 }
        total: { type: integer, format: int64, minimum: 0 }
        total_pages: { type: integer, minimum: 0 }

    CreateStudentRequest:
      type: object
      required: [email, temporary_password, account_status, full_name, status]
      properties:
        email: { type: string, format: email }
        temporary_password: { type: string, format: password, minLength: 8 }
        account_status: { $ref: "#/components/schemas/UserStatus" }
        full_name: { type: string, maxLength: 160 }
        phone: { type: [string, "null"], maxLength: 30 }
        date_of_birth: { type: [string, "null"], format: date }
        gender: { type: [string, "null"], enum: [male, female, other, unspecified, null] }
        address: { type: [string, "null"], maxLength: 500 }
        emergency_contact_name: { type: [string, "null"], maxLength: 160 }
        emergency_contact_phone: { type: [string, "null"], maxLength: 30 }
        status: { $ref: "#/components/schemas/StudentStatus" }
        enrolled_at: { type: [string, "null"], format: date }
    UpdateStudentRequest:
      type: object
      required: [email, account_status, full_name, status]
      properties:
        email: { type: string, format: email }
        account_status: { $ref: "#/components/schemas/UserStatus" }
        full_name: { type: string, maxLength: 160 }
        phone: { type: [string, "null"], maxLength: 30 }
        date_of_birth: { type: [string, "null"], format: date }
        gender: { type: [string, "null"], enum: [male, female, other, unspecified, null] }
        address: { type: [string, "null"], maxLength: 500 }
        emergency_contact_name: { type: [string, "null"], maxLength: 160 }
        emergency_contact_phone: { type: [string, "null"], maxLength: 30 }
        status: { $ref: "#/components/schemas/StudentStatus" }
        enrolled_at: { type: [string, "null"], format: date }
        status_change_reason:
          type: [string, "null"]
          maxLength: 500
          description: Required when status differs from the stored status
    Student:
      type: object
      required: [id, user_id, email, account_status, student_code, full_name, status, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        email: { type: string, format: email }
        account_status: { $ref: "#/components/schemas/UserStatus" }
        student_code:
          type: string
          readOnly: true
          description: New profiles receive an immutable HV + zero-padded sequence code; legacy codes remain readable
        full_name: { type: string }
        phone: { type: [string, "null"] }
        date_of_birth: { type: [string, "null"], format: date }
        gender: { type: [string, "null"], enum: [male, female, other, unspecified, null] }
        address: { type: [string, "null"] }
        emergency_contact_name: { type: [string, "null"] }
        emergency_contact_phone: { type: [string, "null"] }
        status: { $ref: "#/components/schemas/StudentStatus" }
        enrolled_at: { type: [string, "null"], format: date }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    StudentResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/Student" }
        message: { type: string }
    StudentListResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta]
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/Student" } }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
        message: { type: string }

    StudentStatusHistory:
      type: object
      required: [id, student_id, to_status, reason, changed_at]
      properties:
        id: { type: string, format: uuid }
        student_id: { type: string, format: uuid }
        from_status: { type: [string, "null"] }
        to_status: { $ref: "#/components/schemas/StudentStatus" }
        reason: { type: string }
        changed_by: { type: [string, "null"], format: uuid }
        changed_by_email: { type: [string, "null"], format: email }
        changed_at: { type: string, format: date-time }
    StudentStatusHistoryResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: array
          items: { $ref: "#/components/schemas/StudentStatusHistory" }
        message: { type: string }
    StudentImportResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [imported, failed, errors]
          properties:
            imported: { type: integer, minimum: 0 }
            failed: { type: integer, minimum: 0 }
            errors:
              type: array
              items:
                type: object
                required: [row, message]
                properties:
                  row: { type: integer, minimum: 2 }
                  email: { type: string }
                  message: { type: string }
        message: { type: string }

    CreateTeacherRequest:
      type: object
      required: [email, temporary_password, account_status, teacher_code, full_name, status]
      properties:
        email: { type: string, format: email }
        temporary_password: { type: string, format: password, minLength: 8 }
        account_status: { $ref: "#/components/schemas/UserStatus" }
        teacher_code: { type: string, maxLength: 30 }
        full_name: { type: string, maxLength: 160 }
        phone: { type: [string, "null"], maxLength: 30 }
        specialization: { type: [string, "null"], maxLength: 200 }
        status: { $ref: "#/components/schemas/TeacherStatus" }
    UpdateTeacherRequest:
      type: object
      required: [email, account_status, teacher_code, full_name, status]
      properties:
        email: { type: string, format: email }
        account_status: { $ref: "#/components/schemas/UserStatus" }
        teacher_code: { type: string, maxLength: 30 }
        full_name: { type: string, maxLength: 160 }
        phone: { type: [string, "null"], maxLength: 30 }
        specialization: { type: [string, "null"], maxLength: 200 }
        status: { $ref: "#/components/schemas/TeacherStatus" }
    Teacher:
      type: object
      required: [id, user_id, email, account_status, teacher_code, full_name, status, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        email: { type: string, format: email }
        account_status: { $ref: "#/components/schemas/UserStatus" }
        teacher_code: { type: string }
        full_name: { type: string }
        phone: { type: [string, "null"] }
        specialization: { type: [string, "null"] }
        status: { $ref: "#/components/schemas/TeacherStatus" }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    TeacherResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/Teacher" }
        message: { type: string }
    TeacherListResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta]
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/Teacher" } }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
        message: { type: string }

    CourseWriteRequest:
      type: object
      required: [code, name, total_sessions, minimum_attendance_pct, status]
      properties:
        code: { type: string, maxLength: 30 }
        name: { type: string, maxLength: 200 }
        description: { type: [string, "null"] }
        total_sessions: { type: integer, minimum: 1 }
        minimum_attendance_pct: { type: number, const: 80 }
        status: { $ref: "#/components/schemas/CourseStatus" }
    Course:
      type: object
      required: [id, code, name, total_sessions, minimum_attendance_pct, status, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: [string, "null"] }
        total_sessions: { type: integer }
        minimum_attendance_pct: { type: number }
        status: { $ref: "#/components/schemas/CourseStatus" }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CourseResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/Course" }
        message: { type: string }
    CourseListResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta]
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/Course" } }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
        message: { type: string }

    ModuleWriteRequest:
      type: object
      required: [code, name, sequence_no, planned_sessions]
      properties:
        code: { type: string, maxLength: 30 }
        name: { type: string, maxLength: 200 }
        sequence_no: { type: integer, minimum: 1 }
        planned_sessions: { type: integer, minimum: 1 }
        description: { type: [string, "null"] }
    CourseModule:
      type: object
      required: [id, course_id, code, name, sequence_no, planned_sessions, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        course_id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        sequence_no: { type: integer }
        planned_sessions: { type: integer }
        description: { type: [string, "null"] }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    ModuleResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/CourseModule" }
        message: { type: string }
    ModuleListResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/CourseModule" } }
        message: { type: string }

    CompetencyWriteRequest:
      type: object
      required: [code, name, sequence_no]
      properties:
        module_id: { type: [string, "null"], format: uuid }
        code: { type: string, maxLength: 40 }
        name: { type: string, maxLength: 200 }
        description: { type: [string, "null"] }
        is_required: { type: boolean, default: true }
        sequence_no: { type: integer, minimum: 1 }
    CompetencyCriterion:
      type: object
      required: [id, course_id, code, name, is_required, sequence_no, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        course_id: { type: string, format: uuid }
        module_id: { type: [string, "null"], format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: [string, "null"] }
        is_required: { type: boolean }
        sequence_no: { type: integer }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CompetencyResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/CompetencyCriterion" }
        message: { type: string }
    CompetencyListResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/CompetencyCriterion" } }
        message: { type: string }

    ClassWriteRequest:
      type: object
      required: [course_id, class_code, name, start_date, end_date, maximum_students, status]
      properties:
        course_id: { type: string, format: uuid }
        class_code: { type: string, maxLength: 40 }
        name: { type: string, maxLength: 200 }
        start_date: { type: string, format: date }
        end_date: { type: string, format: date }
        maximum_students: { type: integer, minimum: 1 }
        status: { $ref: "#/components/schemas/ClassStatus" }
        change_reason:
          type: string
          maxLength: 500
          description: Required when updating an existing class.
    Class:
      type: object
      required: [id, course_id, course_code, course_name, class_code, name, start_date, end_date, maximum_students, enrolled_students, status, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        course_id: { type: string, format: uuid }
        course_code: { type: string }
        course_name: { type: string }
        class_code: { type: string }
        name: { type: string }
        start_date: { type: string, format: date }
        end_date: { type: string, format: date }
        maximum_students: { type: integer }
        enrolled_students: { type: integer }
        status: { $ref: "#/components/schemas/ClassStatus" }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    ClassResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/Class" }
        message: { type: string }
    ClassListResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta]
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/Class" } }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
        message: { type: string }

    TeacherClassListResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/Class" } }
        message: { type: string }

    TeacherClassDetail:
      type: object
      required: [class, students, competencies]
      properties:
        class: { $ref: "#/components/schemas/Class" }
        students: { type: array, items: { $ref: "#/components/schemas/Enrollment" } }
        competencies: { type: array, items: { $ref: "#/components/schemas/CompetencyCriterion" } }
    TeacherClassDetailResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/TeacherClassDetail" }
        message: { type: string }

    EnrollmentCreateRequest:
      type: object
      required: [student_id]
      properties:
        student_id: { type: string, format: uuid }
        reason: { type: string, maxLength: 500 }
    EnrollmentUpdateRequest:
      type: object
      required: [status, reason]
      properties:
        status: { $ref: "#/components/schemas/EnrollmentStatus" }
        reason: { type: string, minLength: 1, maxLength: 500 }
    EnrollmentTransferRequest:
      type: object
      required: [target_class_id, reason]
      properties:
        target_class_id: { type: string, format: uuid }
        reason: { type: string, minLength: 1, maxLength: 500 }
    Enrollment:
      type: object
      required: [id, class_id, student_id, student_code, full_name, status, enrolled_at, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        class_id: { type: string, format: uuid }
        student_id: { type: string, format: uuid }
        student_code: { type: string }
        full_name: { type: string }
        status: { $ref: "#/components/schemas/EnrollmentStatus" }
        enrolled_at: { type: string, format: date-time }
        ended_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    EnrollmentResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/Enrollment" }
        message: { type: string }
    EnrollmentListResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/Enrollment" } }
        message: { type: string }
    EnrollmentTransfer:
      type: object
      required: [source, target]
      properties:
        source: { $ref: "#/components/schemas/Enrollment" }
        target: { $ref: "#/components/schemas/Enrollment" }
    EnrollmentTransferResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/EnrollmentTransfer" }
        message: { type: string }

    ClassOperationHistory:
      type: object
      required: [id, class_id, event_type, entity_type, details, occurred_at]
      properties:
        id: { type: string, format: uuid }
        class_id: { type: string, format: uuid }
        event_type: { type: string }
        entity_type: { type: string }
        entity_id: { type: [string, "null"], format: uuid }
        reason: { type: [string, "null"], maxLength: 500 }
        details: { type: object, additionalProperties: true }
        actor_user_id: { type: [string, "null"], format: uuid }
        actor_email: { type: [string, "null"], format: email }
        occurred_at: { type: string, format: date-time }
    ClassOperationHistoryListResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/ClassOperationHistory" } }
        message: { type: string }

    AssignmentCreateRequest:
      type: object
      required: [teacher_id, assignment_role]
      properties:
        teacher_id: { type: string, format: uuid }
        assignment_role: { type: string, maxLength: 80 }
        reason: { type: string, maxLength: 500 }
    AssignmentUpdateRequest:
      type: object
      required: [assignment_role, reason]
      properties:
        assignment_role: { type: string, maxLength: 80 }
        reason: { type: string, minLength: 1, maxLength: 500 }
    ReasonRequest:
      type: object
      required: [reason]
      properties:
        reason: { type: string, minLength: 1, maxLength: 500 }
    TeacherAssignment:
      type: object
      required: [id, class_id, teacher_id, teacher_code, full_name, assignment_role, assigned_at, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        class_id: { type: string, format: uuid }
        teacher_id: { type: string, format: uuid }
        teacher_code: { type: string }
        full_name: { type: string }
        assignment_role: { type: string }
        assigned_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    AssignmentResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/TeacherAssignment" }
        message: { type: string }
    AssignmentListResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/TeacherAssignment" } }
        message: { type: string }

    CreateTrainingLocationRequest:
      type: object
      required: [code, name, location_type]
      properties:
        code: { type: string, maxLength: 30 }
        name: { type: string, maxLength: 160 }
        location_type: { type: string, maxLength: 40, example: workshop }
        capacity: { type: [integer, "null"], minimum: 1 }
        is_active: { type: boolean, default: true }
    UpdateTrainingLocationRequest:
      type: object
      required: [code, name, location_type, is_active]
      properties:
        code: { type: string, maxLength: 30 }
        name: { type: string, maxLength: 160 }
        location_type: { type: string, maxLength: 40 }
        capacity: { type: [integer, "null"], minimum: 1 }
        is_active: { type: boolean }
    TrainingLocation:
      type: object
      required: [id, code, name, location_type, is_active, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        location_type: { type: string }
        capacity: { type: [integer, "null"] }
        is_active: { type: boolean }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    TrainingLocationResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/TrainingLocation" }
        message: { type: string }
    TrainingLocationListResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta]
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/TrainingLocation" } }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
        message: { type: string }

    SessionType:
      type: string
      enum: [theory, workshop, assessment, other]
    WritableSessionStatus:
      type: string
      enum: [scheduled, completed, cancelled]
    SessionStatus:
      type: string
      enum: [scheduled, completed, cancelled, locked]
    SessionWriteRequest:
      type: object
      required: [class_id, title, session_type, starts_at, ends_at, status]
      properties:
        class_id: { type: string, format: uuid }
        module_id: { type: [string, "null"], format: uuid }
        teacher_id: { type: [string, "null"], format: uuid }
        location_id: { type: [string, "null"], format: uuid }
        title: { type: string, maxLength: 200 }
        session_type: { $ref: "#/components/schemas/SessionType" }
        starts_at:
          type: string
          format: date-time
          example: "2026-09-01T08:00:00+07:00"
          description: Must start at 08:00, 13:30, or 18:30 in Asia/Ho_Chi_Minh.
        ends_at:
          type: string
          format: date-time
          example: "2026-09-01T12:00:00+07:00"
          description: Must be the matching slot end at 12:00, 17:30, or 21:30 in Asia/Ho_Chi_Minh.
        status: { $ref: "#/components/schemas/WritableSessionStatus" }
        change_reason:
          type: string
          maxLength: 500
          description: Required when rescheduling, cancelling, or otherwise updating a session.
    ClassSession:
      type: object
      required: [id, class_id, class_code, class_name, course_id, course_code, course_name, title, session_type, starts_at, ends_at, status, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        class_id: { type: string, format: uuid }
        class_code: { type: string }
        class_name: { type: string }
        course_id: { type: string, format: uuid }
        course_code: { type: string }
        course_name: { type: string }
        module_id: { type: [string, "null"], format: uuid }
        module_code: { type: [string, "null"] }
        module_name: { type: [string, "null"] }
        teacher_id: { type: [string, "null"], format: uuid }
        teacher_code: { type: [string, "null"] }
        teacher_name: { type: [string, "null"] }
        location_id: { type: [string, "null"], format: uuid }
        location_code: { type: [string, "null"] }
        location_name: { type: [string, "null"] }
        location_type: { type: [string, "null"] }
        title: { type: string }
        session_type: { $ref: "#/components/schemas/SessionType" }
        starts_at:
          type: string
          format: date-time
          description: UTC
        ends_at:
          type: string
          format: date-time
          description: UTC
        status: { $ref: "#/components/schemas/SessionStatus" }
        attendance_locked_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    SessionResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/ClassSession" }
        message: { type: string }
    SessionListResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta]
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/ClassSession" } }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
        message: { type: string }

    AttendanceStatus:
      type: string
      enum: [present, absent, late, excused]
    AttendanceBatchItem:
      type: object
      additionalProperties: false
      required: [student_id, status]
      properties:
        student_id: { type: string, format: uuid }
        status: { $ref: "#/components/schemas/AttendanceStatus" }
        note: { type: [string, "null"], maxLength: 1000 }
    AttendanceBatchRequest:
      type: object
      additionalProperties: false
      required: [records]
      properties:
        records:
          type: array
          minItems: 1
          maxItems: 200
          items: { $ref: "#/components/schemas/AttendanceBatchItem" }
    AttendanceCorrectionRequest:
      type: object
      additionalProperties: false
      required: [status, reason]
      properties:
        status: { $ref: "#/components/schemas/AttendanceStatus" }
        note: { type: [string, "null"], maxLength: 1000 }
        reason: { type: string, minLength: 1, maxLength: 500 }
    AttendanceSession:
      type: object
      required: [id, class_id, class_code, class_name, course_id, course_code, course_name, title, starts_at, ends_at, status]
      properties:
        id: { type: string, format: uuid }
        class_id: { type: string, format: uuid }
        class_code: { type: string }
        class_name: { type: string }
        course_id: { type: string, format: uuid }
        course_code: { type: string }
        course_name: { type: string }
        title: { type: string }
        starts_at: { type: string, format: date-time, description: UTC }
        ends_at: { type: string, format: date-time, description: UTC }
        status: { $ref: "#/components/schemas/SessionStatus" }
        attendance_locked_at: { type: [string, "null"], format: date-time }
    AttendanceRosterItem:
      type: object
      required: [student_id, student_code, full_name, enrollment_status]
      properties:
        student_id: { type: string, format: uuid }
        student_code: { type: string }
        full_name: { type: string }
        enrollment_status: { $ref: "#/components/schemas/EnrollmentStatus" }
        attendance_id: { type: [string, "null"], format: uuid }
        attendance_status:
          anyOf:
            - { $ref: "#/components/schemas/AttendanceStatus" }
            - { type: "null" }
        note: { type: [string, "null"] }
        recorded_by: { type: [string, "null"], format: uuid }
        recorded_by_email: { type: [string, "null"], format: email }
        recorded_at: { type: [string, "null"], format: date-time }
        updated_at: { type: [string, "null"], format: date-time }
    SessionAttendanceSummary:
      type: object
      required: [total, recorded, unrecorded, present, absent, late, excused]
      properties:
        total: { type: integer, minimum: 0 }
        recorded: { type: integer, minimum: 0 }
        unrecorded: { type: integer, minimum: 0 }
        present: { type: integer, minimum: 0 }
        absent: { type: integer, minimum: 0 }
        late: { type: integer, minimum: 0 }
        excused: { type: integer, minimum: 0 }
    SessionAttendance:
      type: object
      required: [session, items, summary]
      properties:
        session: { $ref: "#/components/schemas/AttendanceSession" }
        items:
          type: array
          items: { $ref: "#/components/schemas/AttendanceRosterItem" }
        summary: { $ref: "#/components/schemas/SessionAttendanceSummary" }
    SessionAttendanceResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/SessionAttendance" }
        message: { type: string }
    AttendanceRecord:
      type: object
      required: [id, class_session_id, class_id, student_id, status, recorded_by, recorded_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        class_session_id: { type: string, format: uuid }
        class_id: { type: string, format: uuid }
        student_id: { type: string, format: uuid }
        student_code: { type: string }
        full_name: { type: string }
        status: { $ref: "#/components/schemas/AttendanceStatus" }
        note: { type: [string, "null"] }
        recorded_by: { type: string, format: uuid }
        recorded_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    AttendanceRecordResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/AttendanceRecord" }
        message: { type: string }
    AttendanceBatchResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, count]
          properties:
            items:
              type: array
              items: { $ref: "#/components/schemas/AttendanceRecord" }
            count: { type: integer, minimum: 1, maximum: 200 }
        message: { type: string }
    StudentAttendanceHistoryItem:
      type: object
      required: [id, class_session_id, class_id, class_code, class_name, course_id, course_code, course_name, session_title, starts_at, ends_at, status, recorded_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        class_session_id: { type: string, format: uuid }
        class_id: { type: string, format: uuid }
        class_code: { type: string }
        class_name: { type: string }
        course_id: { type: string, format: uuid }
        course_code: { type: string }
        course_name: { type: string }
        session_title: { type: string }
        starts_at: { type: string, format: date-time }
        ends_at: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/AttendanceStatus" }
        note: { type: [string, "null"] }
        recorded_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    StudentAttendanceHistoryResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta]
          properties:
            items:
              type: array
              items: { $ref: "#/components/schemas/StudentAttendanceHistoryItem" }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
        message: { type: string }
    StudentAttendanceSummary:
      type: object
      required: [class_id, class_code, class_name, course_id, course_code, course_name, recorded_sessions, present_sessions, absent_sessions, late_sessions, excused_sessions, attendance_pct, minimum_attendance_pct, is_at_risk]
      properties:
        class_id: { type: string, format: uuid }
        class_code: { type: string }
        class_name: { type: string }
        course_id: { type: string, format: uuid }
        course_code: { type: string }
        course_name: { type: string }
        recorded_sessions: { type: integer, minimum: 0 }
        present_sessions: { type: integer, minimum: 0 }
        absent_sessions: { type: integer, minimum: 0 }
        late_sessions: { type: integer, minimum: 0 }
        excused_sessions: { type: integer, minimum: 0 }
        attendance_pct:
          type: number
          format: double
          minimum: 0
          maximum: 100
        minimum_attendance_pct:
          type: number
          format: double
          minimum: 0
          maximum: 100
        is_at_risk:
          type: boolean
          description: True after at least one recorded session when attendance is below the course threshold.
    StudentAttendanceSummaryResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items]
          properties:
            items:
              type: array
              items: { $ref: "#/components/schemas/StudentAttendanceSummary" }
        message: { type: string }

    AssessmentStatus:
      type: string
      enum: [draft, submitted, locked]
    CompetencyRating:
      type: string
      enum: [not_assessed, needs_improvement, competent, good, excellent]
    CompletionStatus:
      type: string
      enum: [pending, eligible]
    AssessmentWriteItem:
      type: object
      additionalProperties: false
      required: [competency_criterion_id, rating]
      properties:
        competency_criterion_id: { type: string, format: uuid }
        rating: { $ref: "#/components/schemas/CompetencyRating" }
        comment: { type: [string, "null"], maxLength: 1000 }
    AssessmentWriteRequest:
      type: object
      additionalProperties: false
      properties:
        session_id: { type: [string, "null"], format: uuid }
        overall_comment: { type: [string, "null"], maxLength: 2000 }
        evidence_url: { type: [string, "null"], format: uri, maxLength: 2000 }
        items:
          type: array
          maxItems: 200
          items: { $ref: "#/components/schemas/AssessmentWriteItem" }
    AssessmentItem:
      type: object
      required: [id, competency_criterion_id, criterion_code, criterion_name, is_required, sequence_no, rating, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        competency_criterion_id: { type: string, format: uuid }
        criterion_code: { type: string }
        criterion_name: { type: string }
        is_required: { type: boolean }
        sequence_no: { type: integer, minimum: 1 }
        rating: { $ref: "#/components/schemas/CompetencyRating" }
        comment: { type: [string, "null"] }
        assessed_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    StudentAssessment:
      type: object
      required: [id, class_id, class_code, class_name, course_id, course_code, course_name, student_id, student_code, student_name, assessed_by, teacher_code, teacher_name, assessment_no, status, items, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        class_id: { type: string, format: uuid }
        class_code: { type: string }
        class_name: { type: string }
        course_id: { type: string, format: uuid }
        course_code: { type: string }
        course_name: { type: string }
        student_id: { type: string, format: uuid }
        student_code: { type: string }
        student_name: { type: string }
        assessed_by: { type: string, format: uuid }
        teacher_code: { type: string }
        teacher_name: { type: string }
        session_id: { type: [string, "null"], format: uuid }
        session_title: { type: [string, "null"] }
        assessment_no: { type: integer, minimum: 1 }
        status: { $ref: "#/components/schemas/AssessmentStatus" }
        overall_comment: { type: [string, "null"] }
        evidence_url: { type: [string, "null"], format: uri }
        submitted_at: { type: [string, "null"], format: date-time }
        locked_at: { type: [string, "null"], format: date-time }
        items:
          type: array
          items: { $ref: "#/components/schemas/AssessmentItem" }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    AssessmentResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/StudentAssessment" }
        message: { type: string }
    AssessmentListResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta]
          properties:
            items:
              type: array
              items: { $ref: "#/components/schemas/StudentAssessment" }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
        message: { type: string }
    CourseTestKind:
      type: string
      enum: [class_test, final_exam]
    CourseTestRequest:
      type: object
      additionalProperties: false
      required: [code, title, kind, pass_score, is_required, sequence_no, is_active]
      properties:
        code: { type: string, minLength: 1, maxLength: 40 }
        title: { type: string, minLength: 1, maxLength: 200 }
        kind: { $ref: "#/components/schemas/CourseTestKind" }
        pass_score: { type: number, minimum: 0, maximum: 10, description: "Final exam must use 5 and passing is strictly greater than 5." }
        is_required: { type: boolean }
        sequence_no: { type: integer, minimum: 1 }
        is_active: { type: boolean }
    CourseTest:
      allOf:
        - type: object
          required: [id, course_id]
          properties:
            id: { type: string, format: uuid }
            course_id: { type: string, format: uuid }
        - { $ref: "#/components/schemas/CourseTestRequest" }
    CourseTestResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/CourseTest" }
        message: { type: string }
    CourseTestListResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/CourseTest" } }
        message: { type: string }
    TestAttemptCreateRequest:
      type: object
      additionalProperties: false
      required: [score]
      properties:
        score: { type: number, minimum: 0, maximum: 10 }
        note: { type: [string, "null"], maxLength: 1000 }
        taken_at: { type: [string, "null"], format: date-time }
    TestAttemptCorrectionRequest:
      allOf:
        - { $ref: "#/components/schemas/TestAttemptCreateRequest" }
        - type: object
          required: [reason]
          properties:
            reason: { type: string, minLength: 1, maxLength: 1000 }
    TestAttempt:
      type: object
      required: [id, test_id, class_id, class_code, test_code, test_title, kind, attempt_no, score, pass_score, passed, is_required, recorded_by_email, taken_at]
      properties:
        id: { type: string, format: uuid }
        test_id: { type: string, format: uuid }
        class_id: { type: string, format: uuid }
        class_code: { type: string }
        test_code: { type: string }
        test_title: { type: string }
        kind: { $ref: "#/components/schemas/CourseTestKind" }
        attempt_no: { type: integer, minimum: 1 }
        score: { type: number, minimum: 0, maximum: 10 }
        pass_score: { type: number }
        passed: { type: boolean }
        is_required: { type: boolean }
        note: { type: [string, "null"] }
        recorded_by_email: { type: string }
        taken_at: { type: string, format: date-time }
    TestResult:
      type: object
      required: [test, attempts, passed]
      properties:
        test: { $ref: "#/components/schemas/CourseTest" }
        attempts: { type: array, items: { $ref: "#/components/schemas/TestAttempt" } }
        passed: { type: boolean }
        best_score: { type: [number, "null"] }
    CourseTestResults:
      type: object
      required: [course_id, course_code, course_name, student_id, tests]
      properties:
        course_id: { type: string, format: uuid }
        course_code: { type: string }
        course_name: { type: string }
        student_id: { type: string, format: uuid }
        tests: { type: array, items: { $ref: "#/components/schemas/TestResult" } }
    CourseTestResultsResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/CourseTestResults" }
        message: { type: string }
    StudentCourseTestResultsResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/CourseTestResults" } }
        message: { type: string }
    TestAttemptResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/TestAttempt" }
        message: { type: string }
    TestAttemptHistory:
      type: object
      required: [id, old_score, new_score, reason, changed_by_email, changed_at]
      properties:
        id: { type: string, format: uuid }
        old_score: { type: number }
        new_score: { type: number }
        old_note: { type: [string, "null"] }
        new_note: { type: [string, "null"] }
        reason: { type: string }
        changed_by_email: { type: string }
        changed_at: { type: string, format: date-time }
    TestAttemptHistoryListResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/TestAttemptHistory" } }
        message: { type: string }
    SessionProgressComponent:
      type: object
      required: [completed, required, percent]
      properties:
        completed: { type: integer, minimum: 0 }
        required: { type: integer, minimum: 1 }
        percent: { type: number, minimum: 0, maximum: 100 }
    AttendanceProgressComponent:
      type: object
      required: [records, counted_records, attended, excused, percent, minimum_required_pct, requirement_met]
      properties:
        records: { type: integer, minimum: 0 }
        counted_records: { type: integer, minimum: 0 }
        attended: { type: integer, minimum: 0 }
        excused: { type: integer, minimum: 0 }
        percent: { type: number, minimum: 0, maximum: 100 }
        minimum_required_pct: { type: number, minimum: 0, maximum: 100 }
        requirement_met: { type: boolean }
    CompetencyProgressComponent:
      type: object
      required: [met, required, percent, requirement_met]
      properties:
        met: { type: integer, minimum: 0 }
        required: { type: integer, minimum: 0 }
        percent: { type: number, minimum: 0, maximum: 100 }
        requirement_met: { type: boolean }
    AssessmentProgressComponent:
      type: object
      required: [completed, required, percent, requirement_met]
      properties:
        completed: { type: integer, minimum: 0 }
        required: { type: integer, minimum: 0 }
        percent: { type: number, minimum: 0, maximum: 100 }
        requirement_met: { type: boolean }
    TestProgressComponent:
      type: object
      required: [passed, required, percent, requirement_met]
      properties:
        passed: { type: integer, minimum: 0 }
        required: { type: integer, minimum: 0 }
        percent: { type: number, minimum: 0, maximum: 100 }
        requirement_met: { type: boolean }
    FinalExamProgressComponent:
      type: object
      required: [required_score, requirement_met]
      properties:
        score: { type: [number, "null"], minimum: 0, maximum: 10 }
        required_score: { type: number, const: 5 }
        requirement_met: { type: boolean, description: "True only when score is strictly greater than 5." }
    StudentProgress:
      type: object
      required: [class_id, class_code, class_name, class_status, enrollment_status, course_id, course_code, course_name, sessions, attendance, competencies, assessments, tests, final_exam, failure_reasons, overall_progress_pct, completion_status]
      properties:
        class_id: { type: string, format: uuid }
        class_code: { type: string }
        class_name: { type: string }
        class_status: { $ref: "#/components/schemas/ClassStatus" }
        enrollment_status: { $ref: "#/components/schemas/EnrollmentStatus" }
        course_id: { type: string, format: uuid }
        course_code: { type: string }
        course_name: { type: string }
        sessions: { $ref: "#/components/schemas/SessionProgressComponent" }
        attendance: { $ref: "#/components/schemas/AttendanceProgressComponent" }
        competencies: { $ref: "#/components/schemas/CompetencyProgressComponent" }
        assessments: { $ref: "#/components/schemas/AssessmentProgressComponent" }
        tests: { $ref: "#/components/schemas/TestProgressComponent" }
        final_exam: { $ref: "#/components/schemas/FinalExamProgressComponent" }
        failure_reasons: { type: array, items: { type: string } }
        overall_progress_pct: { type: number, minimum: 0, maximum: 100 }
        completion_status: { $ref: "#/components/schemas/CompletionStatus" }
    ProgressDashboardSummary:
      type: object
      required: [classes, eligible_classes, average_progress_pct]
      properties:
        classes: { type: integer, minimum: 0 }
        eligible_classes: { type: integer, minimum: 0 }
        average_progress_pct: { type: number, minimum: 0, maximum: 100 }
    ProgressDashboard:
      type: object
      required: [items, summary]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/StudentProgress" }
        summary: { $ref: "#/components/schemas/ProgressDashboardSummary" }
    ProgressDashboardResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/ProgressDashboard" }
        message: { type: string }

    CompletionCandidate:
      type: object
      required: [class_id, class_code, class_name, student_id, student_code, student_name, course_code, course_name, completed_sessions, total_sessions, attendance_pct, minimum_attendance_pct, required_competencies_met, required_competencies_total, completed_assessments, required_assessments, required_tests_passed, required_tests_total, final_exam_passed, failure_reasons, is_eligible, status]
      properties:
        class_id: { type: string, format: uuid }
        class_code: { type: string }
        class_name: { type: string }
        student_id: { type: string, format: uuid }
        student_code: { type: string }
        student_name: { type: string }
        course_code: { type: string }
        course_name: { type: string }
        completed_sessions: { type: integer }
        total_sessions: { type: integer }
        attendance_pct: { type: number }
        minimum_attendance_pct: { type: number }
        required_competencies_met: { type: integer }
        required_competencies_total: { type: integer }
        completed_assessments: { type: integer }
        required_assessments: { type: integer }
        required_tests_passed: { type: integer }
        required_tests_total: { type: integer }
        final_exam_score: { type: [number, "null"], minimum: 0, maximum: 10 }
        final_exam_passed: { type: boolean }
        failure_reasons: { type: array, items: { type: string } }
        current_certificate_id: { type: [string, "null"], format: uuid }
        current_certificate_number: { type: [string, "null"] }
        is_eligible: { type: boolean }
        status: { type: string, enum: [pending, eligible, approved, rejected] }
        review_note: { type: [string, "null"] }
        reviewed_at: { type: [string, "null"], format: date-time }
    CompletionCandidateListResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta]
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/CompletionCandidate" } }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
        message: { type: string }
    CompletionDecisionRequest:
      type: object
      additionalProperties: false
      required: [status, note]
      properties:
        status: { type: string, enum: [approved, rejected] }
        note: { type: string, minLength: 1, maxLength: 1000 }
    Certificate:
      type: object
      required: [id, completion_id, certificate_number, verification_code, class_code, class_name, course_code, course_name, student_code, student_name, issued_at, is_current]
      properties:
        id: { type: string, format: uuid }
        completion_id: { type: string, format: uuid }
        certificate_number: { type: string, pattern: '^CC[0-9]{8}$' }
        verification_code: { type: string, format: uuid }
        class_code: { type: string }
        class_name: { type: string }
        course_code: { type: string }
        course_name: { type: string }
        student_code: { type: string }
        student_name: { type: string }
        issued_at: { type: string, format: date-time }
        is_current: { type: boolean }
        revoked_at: { type: [string, "null"], format: date-time }
        revoke_reason: { type: [string, "null"] }
    CertificateActionRequest:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason: { type: string, minLength: 1, maxLength: 1000 }
    CompletionDecisionResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [candidate]
          properties:
            candidate: { $ref: "#/components/schemas/CompletionCandidate" }
            certificate: { oneOf: [{ $ref: "#/components/schemas/Certificate" }, { type: "null" }] }
        message: { type: string }
    CertificateResponse:
      type: object
      required: [data, message]
      properties:
        data: { $ref: "#/components/schemas/Certificate" }
        message: { type: string }
    CertificateListResponse:
      type: object
      required: [data, message]
      properties:
        data: { type: array, items: { $ref: "#/components/schemas/Certificate" } }
        message: { type: string }
    ReportSummaryResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [active_students, open_classes, upcoming_sessions, at_risk_students, approved_completions, pending_notifications]
          properties:
            active_students: { type: integer }
            open_classes: { type: integer }
            upcoming_sessions: { type: integer }
            at_risk_students: { type: integer }
            approved_completions: { type: integer }
            pending_notifications: { type: integer }
        message: { type: string }
    Notification:
      type: object
      required: [id, title, message, type, status, created_at]
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        message: { type: string }
        type: { type: string }
        status: { type: string, enum: [unread, read, archived] }
        action_url: { type: [string, "null"] }
        read_at: { type: [string, "null"], format: date-time }
        created_at: { type: string, format: date-time }
    NotificationListResponse:
      type: object
      required: [data, message]
      properties:
        data:
          type: object
          required: [items, meta, unread]
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/Notification" } }
            meta: { $ref: "#/components/schemas/PaginationMeta" }
            unread: { type: integer }
        message: { type: string }

    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              example: STUDENT_NOT_FOUND
            message:
              type: string
              example: Student not found
            details:
              type: array
              items:
                type: object
                additionalProperties: true
              default: []

# Default security for all business endpoints; system endpoints opt out above.
security:
  - bearerAuth: []
